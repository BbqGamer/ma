%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%% Bachelor's & Master's Thesis Template        %%
%% Copyleft by Dawid Weiss & Marta Szachniuk    %%
%% Faculty of Computing and Telecommunication   %%
%% Poznan University of Technology, 2020        %%
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%


% Szkielet dla pracy licencjackiej pisanej w języku polskim.

\documentclass[english,master,a4paper,oneside]{ppfcmthesis}


\usepackage[utf8]{inputenc}
\usepackage[OT4]{fontenc}
\usepackage{graphicx}
\usepackage{amsmath,amssymb}
\usepackage{tikz}
\usetikzlibrary{shapes,arrows,positioning,calc}

%--------------------------------------
% Strona tytułowa
%--------------------------------------

% Autorzy pracy, jeśli jest ich więcej niż jeden
% wstaw między nimi separator \and
\author{%
   Adam Korba \album{151962}}
\authortitle{}                                % Do not change.

\title{Dynamic Multi-Loss Curriculum Learning with Meta-Optimization}

% Your supervisor comes here.
\ppsupervisor{dr hab.~inż.~Mikołaj Morzy} 

% Year of final submission (not graduation!)
\ppyear{2026}                                 


\begin{document}

% Front matter starts here
\frontmatter\pagestyle{empty}%
\maketitle\cleardoublepage%

%--------------------------------------
% Spis treści
%--------------------------------------

\pagenumbering{Roman}\pagestyle{ppfcmthesis}%
\tableofcontents*
\cleardoublepage % Zaczynamy od nieparzystej strony

%--------------------------------------
% Rozdziały
%--------------------------------------

%Najwygodniej jeśli każdy rozdział znajduje się w oddzielnym pliku
\mainmatter%
\chapter{Introduction}
\label{chap:introduction}

Training deep neural networks is still difficult when the optimization surface
is highly non-convex and contains many poor local minima. In most practical
pipelines, the objective is fixed from the first to the last epoch. Curriculum
learning offers a different view: instead of exposing the full difficulty at
once, training can start from an easier objective and progressively move toward
the final target \cite{elman1993learning,bengio2009curriculum}.

This thesis studies objective-space curricula, with particular focus on
Gaussian continuation. Continuation methods smooth the objective early and then
gradually remove smoothing \cite{allgower1990numerical,mobahi2015theoretical}.
In neural networks this idea is promising, but schedule design is still mostly
manual \cite{wang2021survey}. The central problem addressed here is therefore
how to design and evaluate curriculum schedules that are both effective and
scientifically defensible.

The work combines two complementary perspectives. The first is an
objective-space curriculum track on synthetic non-convex regression surfaces,
inspired by Gaussian continuation. The second is an output-space coarse-to-fine
curriculum track in image classification, introduced to test whether curricula
are more effective when intermediate tasks preserve semantic structure. Across
both tracks, the thesis compares fixed schedules, adaptive schedules, and
hierarchy variants, and places equal emphasis on positive and negative outcomes.

\section{Research questions}

The empirical part is organized around four research questions. They are phrased
so that both positive and negative outcomes are informative.

\begin{description}
    \item[RQ1.] Does objective-space continuation improve regression on
          non-convex target functions after the direct single-target baseline is
          strongly tuned?
    \item[RQ2.] Does output-space coarse-to-fine supervision improve image
          classification, and how does the effect depend on model capacity,
          dataset structure, curriculum length, and hierarchy depth?
    \item[RQ3.] Can adaptive or meta-optimized schedules replace fixed
          hand-designed curricula, or do they introduce new failure modes such
          as staying too long on easy objectives?
    \item[RQ4.] Do trajectory, roughness, and sharpness diagnostics support a
          clear optimization mechanism, or do they only provide correlational
          evidence in selected positive cases?
\end{description}

\section{Contributions}

The main contributions of the thesis are:

\begin{enumerate}
    \item A reproducible training and analysis pipeline for dynamic multi-loss
          curriculum experiments in both regression and image-classification
          settings.
    \item A broad empirical study showing that curriculum effects are real but
          strongly conditional: they depend on capacity, schedule, hierarchy
          quality, and dataset structure.
    \item An adaptive plateau curriculum variant and a matched random-hierarchy
          ablation that separates hierarchy quality from simple label-count
          reduction effects.
    \item A critical synthesis that includes mixed and negative results, not only
          best-case improvements.
\end{enumerate}

\section{Thesis structure}

Chapter~\ref{chap:literature-review} reviews continuation methods, curriculum
learning, multi-objective optimization, and meta-learning. Chapter~\ref{chap:methodology}
defines the common research design, evaluation protocol, and reproducibility
principles. Chapter~\ref{chap:regression-curriculum} presents the
objective-space regression experiments. Chapter~\ref{chap:vision-curriculum}
presents the output-space hierarchical curriculum experiments. Chapter~\ref{chap:discussion}
connects the two empirical tracks and discusses limitations. Detailed
experimental artifacts are provided in the appendices, including the regression
sweeps, CIFAR-100 model-size study, adaptive follow-up, and random-hierarchy
ablation.

\chapter{Background and Related Work}
\label{chap:literature-review}

This chapter reviews the literature behind the methodological choices in this
thesis. The discussion focuses on three lines of work: continuation-based
curriculum design, multi-objective optimization, and meta-learning. The goal is
not to provide an exhaustive survey, but to identify which ideas are mature,
which assumptions are often hidden, and where the open methodological gaps
remain.

\section{Curriculum Learning and Numerical Continuation}
\label{sec:curriculum-continuation}

The idea that models benefit from learning tasks in order of increasing
difficulty was formalized by Bengio et al. \cite{bengio2009curriculum}, drawing
on earlier work by Elman \cite{elman1993learning}. Elman showed that neural
networks with limited capacity could learn complex structures if they started
with simpler tasks. These early constraints are not just shortcuts, but
important for guiding the learning process.

\subsection{Objective-Based Curriculum}
Recent research divides curriculum learning into two main types: instance-based
and objective-based \cite{soviany2022curriculum}. Instance-based methods sort
data by features like noise or complexity, while objective-based approaches
change the definition of the task itself. The latter is closely linked to
numerical continuation methods \cite{allgower1990numerical}, which are used in
global optimization. In these methods, training starts with a smoothed, convex
version of the objective function, making it easier to find a good solution. As
training continues, the smoothing is gradually reduced, allowing the model to
tackle more complex aspects of the problem.

Weinshall et al. \cite{weinshall2018curriculum} give a complementary argument
for instance-based curriculum learning. In a linear-regression setting with SGD,
they define example difficulty using the loss under the optimal hypothesis
rather than the loss under the current model. This is a theoretical device, not
a directly usable training signal: the optimal hypothesis is unknown during
learning, and if it were already available the original supervised-learning
problem would essentially have been solved. Under this oracle definition, easier
examples yield larger expected progress toward the optimum, especially early in
training when the model is still far from a good solution. Their analysis also
separates inherent difficulty from current loss: among examples of equal
inherent difficulty, high current-loss points can still be informative, but
blindly prioritizing all high-loss examples can be harmful because some are
intrinsically noisy or ambiguous. This distinction is useful for this thesis
because it clarifies why curriculum learning and hard-example mining are not the
same strategy.

The practical contribution of Weinshall et al. is therefore to replace the
unobservable oracle ordering with a proxy. They estimate example difficulty with
a separate pretrained teacher model and use that ranking to drive an
easy-to-hard schedule. Their CNN experiments show the strongest gains in early
optimization, with more conditional improvements in final accuracy when the task
is harder, the student network is smaller, or regularization is stronger. The
paper therefore supports a cautious interpretation that aligns with this thesis:
curricula often act by stabilizing the early optimization trajectory, but their
final generalization benefit depends on regime and design choices. At the same
time, their theory is still limited to convex linear regression and the
teacher-derived difficulty score is only a proxy, so the result is best treated
as motivation rather than a complete explanation of curriculum effects in deep
networks.

\subsection{Gaussian Continuation}
One practical way to apply continuation methods in deep learning is through
Gaussian continuation. Mobahi and Fisher \cite{mobahi2015theoretical} analyze
this idea theoretically by embedding a non-convex objective into a family of
Gaussian-smoothed objectives and following a stationary path from the smoothed
problem back to the original one. Their result does not claim that continuation
always finds the global optimum. Instead, it bounds the value reached by the
continuation path using an ``optimization complexity'' term of the objective.
This is important for this thesis because it gives a formal reason why smoothing
can help: objectives with lower continuation complexity have better worst-case
bounds under this procedure. However, the calculable part of this theory is
narrow. Mobahi and Fisher compute the complexity analytically for objectives
represented with Gaussian radial basis functions. That function family is useful
for theory and for illustrating Gaussian smoothing, but it is much less general
than modern neural-network training objectives. In particular, there is no
straightforward way to compute the same complexity measure for a stochastic,
high-dimensional neural-network loss. The result is therefore used here as a
conceptual justification for continuation, not as a practical diagnostic that can
be evaluated for the neural models in this thesis.

Later work by Ilersich et al. \cite{ilersich2025deep} provided further evidence
that Gaussian continuation can transform difficult non-convex landscapes into
ones that are easier to optimize. Empirical studies, such as curriculum by
smoothing \cite{sinha2020curriculum}, use the same intuition in learning
systems: early training suppresses high-frequency or noisy parts of the loss,
and later training restores the full objective. This literature supports the
main design choice in the Gaussian-continuation experiments in this thesis, but
it also highlights a limitation: the smoothing schedule is itself a crucial
algorithmic choice, not a detail that can be ignored.

\subsection{Curriculum Regularization in Scientific Machine Learning}
A related example of objective-based curriculum appears in physics-informed
neural networks. Krishnapriyan et al. \cite{krishnapriyan2021characterizing}
showed that standard PINNs can fail on convection, reaction, and
reaction-diffusion problems even when the neural network architecture is
expressive enough to represent the solution. Their diagnosis is especially
relevant here: the PDE residual used as a soft regularizer can make the loss
landscape more ill-conditioned and harder to optimize. The failure is therefore
not only a matter of model capacity, but also of how the objective is presented
during training.

Their proposed curriculum regularization starts with an easier PDE constraint,
for example a smaller convection or reaction coefficient, and then gradually
increases the coefficient until the target problem is reached. This is a form of
continuation in the objective itself rather than in the data order. On the
reported PINN benchmarks, this curriculum reduced error by one to two orders of
magnitude in regimes where regular PINN training often failed. The connection to
this thesis is conceptual rather than experimental: both approaches treat the
training objective as something that should be introduced progressively. The PINN
results strengthen the motivation for dynamic loss schedules, because they show
that a physically meaningful regularization term can be correct in principle and
still be harmful if imposed at full strength from the start.

Recent robust curriculum methods also operate directly at the loss level.
SuperLoss appends a generic adaptive term to existing objectives and
automatically downweights high-loss samples, which improves robustness in noisy
settings without requiring task-specific training pipelines
\cite{castells2020superloss}. At the evaluation level, CurBench established a
unified benchmark and toolkit for curriculum learning across vision, language,
and graph domains, making cross-method comparisons more reproducible
\cite{zhou2024curbench}.

Despite these advances, most existing methods rely on fixed schedules for
adjusting the level of smoothing or regularization. These schedules are often
chosen manually and do not adapt to the model's progress \cite{wang2021survey}.
This limitation motivates the development of automated, data-driven scheduling
methods, which is a central focus of this thesis.

\subsection{Curriculum Learning versus Continual Learning}

The abbreviation ``CL'' is overloaded: it can mean \emph{curriculum learning}
or \emph{continual learning}. To avoid this ambiguity, this thesis writes both
terms in full in the prose and uses only short table labels such as ``curr.''
when space is limited.

The distinction is important. Curriculum learning is a training strategy for a
chosen target objective: the learner is deliberately exposed to easier examples,
auxiliary tasks, smoother objectives, or coarser labels before returning to the
final target. The intermediate stages are scaffolding. They do not necessarily
need to remain independently solved after the final stage. Continual learning,
by contrast, is usually a problem setting in which the data distribution,
classes, or tasks arrive over time and the learner must acquire new competence
without catastrophically forgetting earlier competence
\cite{kirkpatrick2017overcoming,lopezpaz2017gradient,parisi2019continual}.

The two ideas nevertheless connect naturally. Both involve non-i.i.d. training
trajectories and both raise questions about transfer between tasks, ordering,
and stability versus plasticity. In reinforcement learning, for example,
curriculum learning is often formulated explicitly as the design of a sequence
of source tasks that should improve learning on a target task
\cite{narvekar2020curriculum}. Teacher--student curriculum methods similarly
select tasks according to the learner's progress \cite{matiisen2020teacher}.
This thesis is closer to curriculum learning than to continual learning because
the final target objective is fixed in advance, but continual-learning language
is useful for discussing when the model should preserve or abandon information
from earlier stages.

\section{Multi-Task and Multi-Objective Optimization}

To move between objectives of different difficulty, the problem can be set up
as a multi-task learning scenario. Here, the total loss is a weighted sum of
several component losses. However, curriculum learning and multi-task learning
have different goals when it comes to how these tasks interact.

\subsection{Gradient Conflict and Balancing}
In multi-task learning, the usual goal is to balance progress across all tasks.
One common problem is negative transfer, where learning one task can interfere
with another. Methods like GradNorm \cite{chen2018gradnorm} adjust task weights
to keep gradient magnitudes similar, while PCGrad \cite{yu2020gradient}
modifies gradients to reduce interference. Sener and Koltun
\cite{sener2018multi} proposed the Multi-Gradient Descent Algorithm (MGDA),
which seeks a solution where no task can be improved without hurting another.
Surveys such as \cite{chen2024gradient} discuss these approaches and note that,
while they help balance tasks, they do not support the sequential learning
needed for curricula.

\subsection{The Equilibrium-Disequilibrium Conflict}
Curriculum learning requires the model to focus on easier tasks first and
gradually shift to harder ones, rather than balancing all tasks equally.
Standard balancing methods often fail here because they might upweight the hard
task too early if its gradient magnitude is small, or maintain the easy task's
weight indefinitely. This is also where curriculum learning differs from
continual learning. A continual learner is usually evaluated on whether it
retains old tasks while learning new ones; a curriculum learner may instead need
to reduce or abandon the auxiliary easy task once it has served its purpose.
The relevant design problem is therefore not only gradient balancing, but
controlled disequilibrium: when should the optimizer be pushed away from the
easy objective and toward the final target?

\subsection{Pareto Continual Learning}
Bridging this gap, \cite{lai2025pareto} introduced \textbf{Pareto Continual
	Learning}, which reformulates the stability-plasticity trade-off (retaining old
knowledge vs. learning new tasks) as a Multi-Objective Optimization (MOO)
problem. Instead of finding a single fixed weight, they learn a set of
Pareto-optimal solutions conditioned on a preference vector. This allows the
system to dynamically select the optimal trade-off during inference. Similarly,
\cite{mao2022metaweighting} proposed \textbf{MetaWeighting}, identifying a
critical gap between training loss and generalization loss in MTL. By treating
task weights as hyperparameters optimized via a bi-level optimization loop
(meta-learning), MetaWeighting explicitly minimizes generalization loss. This
distinction is vital for this thesis: we do not merely want to balance the
gradients of the smoothed and sharp losses; we want to schedule them to
maximize final validation performance.

\section{Meta-Learning and Continual Adaptation}
\label{sec:meta-learning-continual}

Meta-learning, or ``learning to learn,'' provides the algorithmic engine to
automate the curriculum schedule. While early meta-learning focused on learning
initializations (e.g., MAML \cite{finn2017model}) for few-shot adaptation, the
field has expanded into \textbf{Meta-Continual Learning (MCL)}.

\subsection{Taxonomy of Meta-Continual Frameworks}
A recent survey by \cite{son2024meta} provides a rigorous taxonomy for the
intersection of these fields. They distinguish between \textit{Continual
	Meta-Learning} (learning to learn from a sequence of tasks) and
\textit{Meta-Continual Learning} (using meta-learning to solve the catastrophic
forgetting problem). Our research aligns with their definition of
\textbf{Continual Bi-Level Learning}, where the optimization process itself is
updated online.

\subsection{Meta-Optimization of Learning Dynamics}
In this framework, the learning process is split into two loops. The inner loop
updates model parameters $\theta$ on the current curriculum stage via standard
gradient descent. The outer loop updates meta-parameters $\phi$ (in our case,
the loss weights) to minimize a validation loss. Algorithms like
\textbf{La-MAML} \cite{gupta2020lamaml} employ this structure to learn
per-parameter learning rates that align gradients across sequential tasks.
\cite{wu2024meta} recently improved upon this with \textbf{VR-MCL}, introducing
variance reduction techniques to stabilize the online Hessian approximation
required for these bi-level updates.

Furthermore, \cite{woo2025meta} extended MCL to \textbf{Neural Fields}
(MCL-NF). They demonstrated that meta-learning could efficiently reconstruct
complex 3D scenes and signals by dynamically modulating the network's capacity,
a process analogous to the resolution-based curriculum proposed in this thesis.

\subsection{The Stability-Plasticity Dilemma in Curriculum}
A unique challenge arises when applying meta-learning to curriculum generation:
the stability-plasticity dilemma. In standard continual learning, the goal is
to prevent the forgetting of old tasks (stability) \cite{parisi2019continual}.
In our curriculum context, the dilemma is inverted: the model must be
``plastic'' enough to abandon the smoothed approximation (the easy task) and
``stable'' enough to converge on the hard task.

\cite{banayeeanzade2025theoretical} provides theoretical insights into overparameterized models in such multi-task setups, suggesting that without explicit regularization, the interplay between model capacity and task similarity determines the success of transfer. Pure meta-learning of loss weights can be unstable; without constraints, a meta-learner might exploit the easy smoothed loss to achieve artificially low training error without ever transitioning to the hard target function. This necessitates the monotonic regularization strategies explored in this thesis.

\section{Output-Space Curriculum and Optimization Diagnostics}

\subsection{Learning to Reweight Examples}
Ren et al. \cite{renLearningReweightExamples2018a} proposed a bilevel procedure
that reweights training examples according to their short-term effect on a clean
validation set. Although their work targets noisy labels rather than curriculum
scheduling, it is directly relevant methodologically: it shows how a validation
objective can control inner-loop updates without manually defining static
weights.

\subsection{Coarse-to-Fine Curriculum in Output Space}
Stretcu et al. \cite{stretcuCoarsetoFineCurriculumLearning2021} introduced a
coarse-to-fine curriculum in the output space. Instead of reordering training
examples, they cluster fine labels into progressively finer groups and train the
model from coarse supervision to the final class labels. This is a key
reference for the image-classification experiments in this thesis, especially
for analyzing how hierarchy quality, hierarchy depth, and curriculum length
influence the result.

\subsection{Sharpness-Aware Minimization and Flatness Probes}
Foret et al. \cite{foretSharpnessAwareMinimizationEfficiently2021} linked
generalization to neighborhood-aware optimization through the
Sharpness-Aware Minimization (SAM) objective. This thesis does not optimize SAM
directly, but uses roughness and sharpness diagnostics inspired by the same
geometry-based perspective. The practical question is whether curriculum gains
coincide with flatter regions of the loss landscape.

\section{Synthesis}

The literature supports the core design of this thesis: continuation methods
motivate easy-to-hard objective transitions
\cite{allgower1990numerical,mobahi2015theoretical,ilersich2025deep},
multi-objective methods provide the machinery to combine losses
\cite{chen2024gradient}, and meta-learning offers mechanisms for dynamic
weighting \cite{son2024meta,mao2022metaweighting}.

At the same time, the review exposes clear gaps. Most practical curricula still
use fixed schedules. Standard multi-task balancing aims at simultaneous
equilibrium, not sequential transition. Meta-learning methods can adapt weights,
but without constraints they may overuse easy objectives and delay transfer to
the final task. The empirical part of this thesis is therefore organized around
these unresolved points: when curriculum helps, when it fails, and which design
choices are responsible.

\chapter{Methodology}
\label{chap:methodology}

This chapter defines the common methodological frame used in both empirical
tracks. Dataset-specific details are presented together with the corresponding
results in Chapters~\ref{chap:regression-curriculum} and
\ref{chap:vision-curriculum}. The chapter also separates algorithmic choices
from engineering choices. Infrastructure details such as experiment tracking or
container execution are included only where they affect reproducibility,
provenance, or the feasibility of running comparable experiments.

\section{Research design}

The thesis treats curriculum learning as a controlled modification of the
training objective. A curriculum method is considered useful only if it improves
model quality or optimization behavior relative to a strong non-curriculum
baseline trained under comparable conditions. This requirement is important
because weak baselines can make curriculum effects appear larger than they are.

The empirical work is organized into two tracks. The first track studies
objective-space curricula for regression, where the target function is smoothed
and gradually sharpened. The second track studies output-space curricula for
classification, where coarse labels are replaced by progressively finer labels.
The second track is motivated by the limitations of the first: smoothing can make
optimization easier, but it may also remove information that is needed by the
final task.

The design is deliberately comparative rather than purely constructive. The goal
is not only to show that a curriculum can work, but also to identify when it does
not work. For this reason, negative results, runtime costs, trajectory penalties,
and ablations are treated as part of the evidence rather than as failed
experiments.

\section{Evidence hierarchy}

The experiments are interpreted using an explicit hierarchy of evidence. The
strongest claims require matched baselines, comparable training budgets, and at
least a small amount of seed replication. Single-seed sweeps are used primarily
for screening and for selecting promising regions of the design space. Diagnostic
plots, roughness probes, and best-run examples are treated as explanatory
support, not as standalone evidence.

This distinction matters because several results in the thesis use best test
accuracy along a trajectory. Such a metric is useful for understanding whether a
training procedure ever reaches a better solution, but it is weaker than a
validation-selected test result. Consequently, the thesis separates claims about
\emph{peak quality} from claims about deployable model selection or training
speed.

\section{Curriculum learning as dynamic loss optimization}

Both tracks can be interpreted as dynamic loss optimization. At training step
$t$, the model parameters $\theta$ are updated using a loss
\begin{equation}
    \mathcal{L}_t(\theta) = \sum_{k=1}^{K} w_k(t)\mathcal{L}_k(\theta),
\end{equation}
where $\mathcal{L}_k$ are task- or resolution-specific losses and $w_k(t)$ are
schedule weights. In fixed curricula, these weights are predefined. In adaptive
or meta-optimized variants, they depend on training progress or validation
performance.

In the regression track, the component losses correspond to different smoothed
versions of the same target function. In the classification track, the component
losses correspond to different levels of label granularity. This shared view is
useful because it makes the two tracks comparable: both ask whether an easier
objective can guide optimization toward a better solution on the final objective.

\section{Regression track}

The regression experiments use synthetic benchmark functions with non-convex
structure. For each function, the training data contain the original target and a
sequence of Gaussian-smoothed targets. The main baseline is direct training on
the final, least-smoothed target. Curriculum variants train on a sequence or a
weighted mixture of smoothed objectives before emphasizing the final target.

Hyperparameter sweeps are part of the methodology rather than an implementation
detail. They reduce the risk of comparing curriculum methods against an
under-tuned single-target baseline. The regression track therefore first
establishes strong baseline configurations and only then evaluates curriculum or
multi-loss variants against them.

\section{Vision track}

The image-classification track uses output-space curricula inspired by
coarse-to-fine label hierarchies. Instead of smoothing the input or target
surface, the method replaces the final label set with coarser label groups early
in training. Later stages introduce finer labels until the original
classification problem is restored.

The hierarchy is learned from model-derived class information, primarily the
geometry of classifier weights after baseline training. The main comparison is
between direct fine-label training and curriculum training with the learned
hierarchy. Additional controls test whether the effect depends on schedule
length, model capacity, dataset difficulty, hierarchy depth, and hierarchy
quality. The random-hierarchy ablation is especially important because it keeps
the amount of early label coarsening fixed while destroying the learned class
grouping.

\section{Baselines and controls}

The main baseline in both tracks is direct training on the final target objective
or final label space. Curriculum variants are compared against this baseline
using matched architectures, training budgets, data splits, and, where possible,
hyperparameter search. The experiments also include several controls:

\begin{itemize}
    \item model-capacity comparisons, especially in the CIFAR-100 vision track,
    \item fixed versus adaptive curriculum schedules,
    \item shallow versus deeper hierarchy usage,
    \item cross-dataset checks on easier and harder datasets,
    \item random hierarchy controls with matched cluster sizes.
\end{itemize}

These controls are necessary because a curriculum can help for several different
reasons: it may provide meaningful structure, reduce early-task complexity,
regularize optimization, or simply delay hard-label training. The methodology is
therefore designed to separate these explanations where possible.

\section{Evaluation protocol}

The primary metrics are final validation or test performance, best achieved
performance during training, and trajectory-based diagnostics such as the area
under the test-accuracy-over-epoch curve. When this thesis writes AUC, it means
this normalized area under a learning trajectory, computed with the trapezoidal
rule over epochs; it does \emph{not} mean ROC AUC. For the regression track, the
main quality measure is prediction error on held-out samples. For the
classification track, the main quality measure is top-1 accuracy. Additional
classification metrics, including macro-F1, top-5 accuracy, calibration error,
and hierarchy-aware scores, are used where they clarify whether accuracy gains
reflect broader improvements.

Best achieved test performance is useful for understanding training dynamics,
but it is retrospective. It should not be confused with a deployment protocol in
which the epoch is selected using validation data and the test set is evaluated
once. For this reason, the discussion distinguishes peak-accuracy evidence from
trajectory evidence. A curriculum that reaches a higher peak but has lower AUC is
interpreted as improving the best solution found, not as accelerating training.

Roughness, Hessian, and sharpness probes are treated as diagnostics rather than
as proof of mechanism. They can support or weaken a flattening interpretation,
but they do not by themselves establish that curriculum causes the optimizer to
find flatter minima.

\section{Experiment tracking and reproducibility}

The implementation uses scripted experiment entry points, explicit configuration
files or command-line arguments, fixed random seeds where possible, and stored
run metadata. Optuna \cite{akiba2019optuna} is used for several
hyperparameter and schedule searches. The regression track uses MLflow
\cite{zaharia2018accelerating} to record parameters, metrics, artifacts, and
run structure. The vision track uses Weights \& Biases \cite{biewald2020experiment}
for the same purpose; W\&B is the source of truth for the later CIFAR-100
\cite{krizhevsky2009learning}, Fashion-MNIST \cite{xiao2017fashion}, CIFAR-10
\cite{krizhevsky2009learning}, and Tiny ImageNet \cite{tinyimagenet,deng2009imagenet}
analyses. Tables and figures in the appendices are derived from exported
tracking data rather than from manually copied console logs.

The large vision experiments were executed in a containerized environment. The
Docker image included the PyTorch \cite{paszke2019pytorch} and CUDA-related
dependencies required by the training code, was versioned and pushed to Docker
Hub, and was then used on Runpod GPU instances to run consecutive experiment
batches. This is not an algorithmic contribution, and it is not used as
evidence that the method works.
It is reported because it makes the compute environment and dependency set more
stable across long-running experiments, which is important for reproducibility
and for interpreting failed or repeated runs.

Detailed run reports are kept in the appendices so that the main text can focus
on the research claims while preserving the connection to the actual
experiments. Local analysis folders and raw tracking exports are treated as
scratch material unless a figure or table is intentionally incorporated into the
thesis.


\chapter{Objective-Space Curriculum for Regression}
\label{chap:regression-curriculum}

This chapter presents the original empirical track of the thesis: curriculum
learning through objective-space continuation on synthetic non-convex regression
surfaces. The goal was to test whether Gaussian-inspired smoothing and dynamic
multi-loss schedules make difficult target functions easier to learn. The main
question for this track is RQ1: whether continuation still helps after the direct
single-target baseline has been tuned carefully.

\section{Research question and experimental logic}

The regression track evaluates a narrow hypothesis: if a target surface is hard
because of non-convex local structure, then training first on smoother versions
of the same surface may guide the optimizer toward better final solutions. This
hypothesis is tested against a direct hard-target baseline, not against an
untuned default model. The track therefore proceeds in three steps:

\begin{enumerate}
    \item tune model and optimizer hyperparameters for each benchmark function,
    \item compare direct single-target training with fixed continuation
          schedules under matched conditions,
    \item test whether adaptive multi-loss weighting can improve on fixed or
          single-target training in a difficult representative case.
\end{enumerate}

\section{Benchmark functions and continuation targets}

The experiments use eight two-dimensional synthetic regression functions:
\texttt{ackley}, \texttt{levy}, \texttt{bukin}, \texttt{eggholder},
\texttt{franke}, \texttt{peaks}, \texttt{friedman1\_2d}, and
\texttt{friedman2\_2d}. For each function, data are generated from the
analytical target and stored as train and test parquet files. The continuation
variants use multiple target columns corresponding to different Gaussian
smoothing levels. The hardest level is the original or least-smoothed target and
is the metric used for final comparison.

The later v3 setting introduced a stricter protocol: 20,000 generated samples
per function, a 10,000/10,000 train--test split, an internal 80/20 train--validation
split inside the training file, additive Gaussian label noise, three smoothing
levels, and an explicit parameter-to-training-samples constraint. These details
are kept in Appendix~\ref{app:sweep-pack-v3-setup}, while the main text uses
them to motivate why baseline strength and model capacity are treated as
methodological variables.

\section{Baseline tuning protocol}

Before comparing curricula, the first sweep pack optimized architecture and
training hyperparameters separately for each function. The search covered MLP,
SIREN \cite{sitzmann2020implicit}, and Fourier-feature networks
\cite{tancik2020fourier} and optimized validation MSE. The result was not a
single universal model family: MLPs were strongest overall, Fourier features
were best on \texttt{eggholder}, and SIREN was best on \texttt{peaks}. This
justified using per-function best configurations in the subsequent curriculum
comparison instead of imposing one manual architecture on all functions.

\section{Fixed continuation versus direct training}

The main fixed-schedule comparison used a factorial design with two methods
(single and curriculum), two budget regimes (equal epochs and equal time), eight
functions, and three seeds, for 96 completed runs. Table~\ref{tab:main-regression-wins}
summarizes the result in the main text; the full report is given in
Appendix~\ref{app:curr-vs-single-v1}.

\begin{table}[h]
\centering
\small
\begin{tabular}{lrrrr}
\toprule
Regime & Quality wins: curriculum & Quality wins: single & Runtime wins: single & Runtime wins: curriculum \\
\midrule
equal epochs & 5 & 3 & 8 & 0 \\
equal time & 3 & 5 & 8 & 0 \\
\bottomrule
\end{tabular}
\caption{Main regression benchmark summary. Quality wins count functions where
the median hard-validation loss favored the method. Runtime wins count median
runtime winners.}
\label{tab:main-regression-wins}
\end{table}

The result is mixed. Curriculum helped quality on selected functions such as
\texttt{bukin}, \texttt{franke}, and \texttt{levy}, but it did not help on
\texttt{ackley}, \texttt{eggholder}, or \texttt{peaks}. It also never won on
runtime. This means that continuation cannot be treated as a default replacement
for direct hard-target training. Its value is conditional on task geometry and
must be weighed against extra computation.

\section{Adaptive multi-loss weighting}

The adaptive weighting follow-up tested whether gradient-based loss weighting
over multiple continuation targets could overcome the limitations of fixed
schedules. The most stable completed benchmark used \texttt{eggholder}, a
one-step meta lookahead, four random seeds, three noise levels, multiple model
and meta learning rates, and \texttt{1, 2, 3, 5} losses. All 288 runs completed,
which shows that the one-step formulation is operationally feasible.

However, the direct hard-target baseline remained strongest on average.
Table~\ref{tab:main-meta-eggholder-summary} integrates the central result from
Appendix~\ref{app:meta-weighting-eggholder-u1}.

\begin{table}[h]
\centering
\small
\begin{tabular}{lrrr}
\toprule
Losses & Mean improvement vs. one-loss baseline & Median improvement & Win rate \\
\midrule
2 & -1259.72 & -1089.23 & 0.458 \\
3 &  -324.00 &  -549.98 & 0.472 \\
5 & -1777.98 & -1305.75 & 0.417 \\
\bottomrule
\end{tabular}
\caption{Adaptive meta-weighting on \texttt{eggholder}. Positive improvement
would mean that adaptive weighting beats the direct hard-target baseline; all
mean improvements are negative.}
\label{tab:main-meta-eggholder-summary}
\end{table}

Adaptive weighting occasionally produced very strong individual runs, especially
with two or three losses and higher model learning rates, but this was not stable
across seeds and noise settings. The result narrows the claim: adaptive
multi-loss continuation is feasible and interpretable, but it is not yet a robust
replacement for a tuned single-target baseline.

\section{Track-level conclusion}

The regression track answers RQ1 cautiously. Objective-space continuation can
improve final quality on some non-convex surfaces, but the effect is not general
and comes with runtime cost. The strongest methodological lesson is that
curriculum claims are fragile unless the direct baseline is strong. This finding
motivated the second track: if simple smoothing is not reliably beneficial, a
more structured curriculum may be needed, where intermediate tasks preserve
semantic information rather than merely removing high-frequency detail.


\chapter{Output-Space Hierarchical Curriculum for Vision}
\label{chap:vision-curriculum}

This chapter presents the second empirical track of the thesis. It moves from
smoothing regression targets to coarse-to-fine supervision in image
classification. The goal is to test whether curriculum learning is more useful
when intermediate tasks preserve semantic structure instead of only reducing
objective roughness. This chapter addresses RQ2 and RQ3 most directly.

\section{Research question and experimental logic}

The vision track tests a different form of easy-to-hard training. Instead of
changing the input data or smoothing a regression target, the model first learns
a coarser output space and later returns to the original fine-label problem. The
central hypothesis is that coarse labels can provide useful intermediate
structure when the final label space is large enough and the model has limited
capacity.

This track was designed as a sequence of increasingly strict controls:

\begin{enumerate}
    \item a CIFAR-100 model-size screening sweep,
    \item multi-seed fixed and adaptive schedule follow-ups,
    \item dataset controls on CIFAR-10, Fashion-MNIST, and Tiny ImageNet,
    \item hierarchy inspection and a matched random-hierarchy ablation.
\end{enumerate}

\section{Coarse-to-fine training method}

The implementation follows the continuous coarse-to-fine formulation: the model
keeps a fine-class classifier head, but the training loss is evaluated after
marginalizing fine-label probabilities into the active coarse clusters. The
hierarchy is generated from model-derived class similarity, primarily the
classifier-weight geometry of a baseline model. Thus, the hierarchy is not a
manual taxonomy; it is a model-specific estimate of which classes are close in
the learned representation.

A fixed curriculum allocates a predetermined number of epochs to coarse labels
before fine-label training. The adaptive plateau curriculum starts at a coarse
level and advances when fine-label validation accuracy stops improving after a
minimum number of epochs or when a maximum stage length is reached. This rule is
intended to reduce manual curriculum-length tuning, but the experiments below
show that it can also stay too long in coarse stages.

\section{CIFAR-100 model-size study}

The first screening study used 80 completed CIFAR-100 runs
\cite{krizhevsky2009learning}: one baseline and five curriculum lengths for
each model configuration, mostly with seed 42. The training budget was 100
epochs with Adam \cite{kingma2015adam}, learning rate 0.001, and curriculum
lengths 5, 10, 20, 30, and 40. The full table is in
Appendix~\ref{app:cifar100-model-size-curriculum}; Table~\ref{tab:main-cifar100-capacity}
keeps the core pattern in the main text.

\begin{table}[h]
\centering
\small
\begin{tabular}{lrrrr}
\toprule
Model & Parameters & Baseline & Best curriculum & Gain \\
\midrule
\texttt{cnn\_w0.5} & 65,636 & 34.78 & 37.96 & +3.18 \\
\texttt{cnn\_w1} & 158,820 & 38.49 & 42.11 & +3.62 \\
\texttt{cnn\_w2} & 428,132 & 42.56 & 44.72 & +2.16 \\
\texttt{cnn\_w4} & 1,298,532 & 44.63 & 44.47 & -0.16 \\
\texttt{cifar\_resnet8} & 83,892 & 52.68 & 54.49 & +1.81 \\
\texttt{cifar\_resnet56} & 861,620 & 63.14 & 62.33 & -0.81 \\
\texttt{resnet18} & 11,220,132 & 68.76 & 69.37 & +0.61 \\
\bottomrule
\end{tabular}
\caption{Selected CIFAR-100 model-size results. Accuracies are percentages and
gains are percentage points relative to the matching baseline.}
\label{tab:main-cifar100-capacity}
\end{table}

The strongest gains occur for smaller CNNs. Within the CNN family, increasing
width changes the best schedule from moderate length to short length and finally
to no useful curriculum. This supports a capacity-conditioned interpretation:
coarse supervision helps most when the model is not already strong enough to
learn the fine task directly.

The same screening study also showed that curriculum did not improve the whole
training trajectory. AUC gains were negative even for models whose peak accuracy
improved. Therefore, the curriculum effect should be described as a possible
improvement in the best solution reached, not as faster training.

\section{Fixed schedules, adaptive schedules, and hierarchy depth}

The follow-up repeated important CIFAR-100 cases over seeds 42, 43, and 44 and
added adaptive plateau schedules. Table~\ref{tab:main-cifar100-policy} summarizes
the central comparison from Appendix~\ref{app:roughness-followup-curriculum}.

\begin{table}[h]
\centering
\small
\begin{tabular}{lrrr}
\toprule
Model & Fixed best & Adaptive all levels & Adaptive first level \\
\midrule
\texttt{cnn\_w0.5} & \textbf{37.58 (+2.54)} & 37.19 (+1.75) & 37.22 (+2.24) \\
\texttt{cnn\_w1} & \textbf{40.60 (+1.98)} & 39.35 (+0.79) & 40.23 (+1.59) \\
\texttt{cnn\_w2} & \textbf{44.15 (+1.72)} & 39.80 (-2.70) & -- \\
\texttt{cnn\_w4} & \textbf{44.85 (+0.34)} & 39.71 (-4.55) & -- \\
\texttt{cifar\_resnet8} & \textbf{56.60 (+1.33)} & 55.77 (+0.09) & 55.64 (+0.72) \\
\bottomrule
\end{tabular}
\caption{CIFAR-100 policy comparison. Entries are mean best test accuracy over
available seeds, with gain over the matching baseline in parentheses.}
\label{tab:main-cifar100-policy}
\end{table}

Fixed schedules remain stronger than the current adaptive plateau rule. The
adaptive method is not useless, but it often allocates too many epochs to coarse
levels, especially for larger CNNs whose best fixed schedule is very short. The
first-level-only adaptive variant is consistently better than using all generated
levels where both were tested, showing that hierarchy depth is a first-order
design choice rather than a harmless implementation detail.

\section{Dataset controls}

The same curriculum idea was tested on easier or different datasets. CIFAR-10
\cite{krizhevsky2009learning} shows small positive gains for weak CNNs,
Fashion-MNIST \cite{xiao2017fashion} is essentially neutral or negative, and
Tiny ImageNet \cite{tinyimagenet,deng2009imagenet} is mixed: small Adam-trained
models were negative, while a stronger ResNet-18 \cite{he2016deep} with
SGD/cosine \cite{loshchilov2017sgdr} achieved a modest positive gain. The
important conclusion is that weak model capacity alone is not sufficient. The
dataset must also contain class structure that is useful at coarse granularity.

\section{Hierarchy-quality ablation}

The final control asks whether the learned hierarchy matters, or whether any
coarse label reduction would have produced a similar gain. The ablation compares
the learned CIFAR-100 hierarchy for \texttt{cnn\_w0.5} with random hierarchies
that preserve the same tree shape and cluster sizes. This keeps the amount of
early label coarsening fixed while destroying the learned class grouping.

\begin{table}[h]
\centering
\small
\begin{tabular}{lrrrr}
\toprule
Condition & Best accuracy & Accuracy gain & AUC & AUC gain \\
\midrule
Learned hierarchy & 37.53 & +2.24 & 32.75 & -3.83 \\
Random hierarchy mean & 35.81 & +0.53 & 31.75 & -5.39 \\
Best random hierarchy & 36.34 & +1.05 & -- & -- \\
\bottomrule
\end{tabular}
\caption{CIFAR-100 \texttt{cnn\_w0.5} hierarchy ablation. The learned hierarchy
beats matched random hierarchies, but both curriculum variants pay a trajectory
AUC cost.}
\label{tab:main-hierarchy-ablation}
\end{table}

The learned hierarchy outperforms the random mean for all three training seeds
and also beats the best-of-three random diagnostic. This supports the claim that
hierarchy quality matters: the curriculum gain is not explained only by reducing
the number of labels early in training.

\section{Track-level conclusion}

The vision track answers RQ2 positively but conditionally. Coarse-to-fine
curriculum learning can improve peak accuracy for lower-capacity models on
structured many-class datasets, especially CIFAR-100. The effect weakens on
larger models, easier datasets, and poorly matched hierarchies. It also answers
RQ3 negatively for the current adaptive rule: adaptive plateau scheduling reduces
manual tuning in principle, but in these experiments it does not outperform
well-chosen fixed schedules.

\chapter{Discussion}
\label{chap:discussion}

This chapter connects the two empirical tracks and summarizes the strongest
claims supported by the experiments. Table~\ref{tab:main-findings-overview}
provides a compact overview.

\begin{table}[h]
\centering
\small
\begin{tabular}{p{0.23\textwidth}p{0.23\textwidth}p{0.42\textwidth}}
\hline
Question & Strongest evidence & Current verdict \\
\hline
Capacity sensitivity & Appendices~\ref{app:cifar100-model-size-curriculum} and \ref{app:roughness-followup-curriculum} & Supported. Gains are strongest for weaker CNNs and smaller ResNets, and they shrink for larger models. \\
Fixed vs adaptive schedules & Appendix~\ref{app:roughness-followup-curriculum} & Fixed schedules remain stronger than the current adaptive plateau rule on CIFAR-100. \\
Hierarchy quality (learned vs random) & Appendix~\ref{app:hierarchy-ablation-curriculum} & Supported in the main CIFAR-100 weak-CNN setting: learned hierarchy +2.24 pp vs random mean +0.53 pp. \\
Trajectory speed (AUC) & Appendices~\ref{app:cifar100-model-size-curriculum}, \ref{app:roughness-followup-curriculum}, \ref{app:hierarchy-ablation-curriculum} & Mostly negative for curriculum. Peak accuracy can improve while early-trajectory performance drops. \\
Cross-dataset transfer & Appendix~\ref{app:roughness-followup-curriculum} & Mixed. CIFAR-100 is strongest, CIFAR-10 gains are smaller, Fashion-MNIST is mostly neutral/negative, Tiny ImageNet is setup-sensitive. \\
Roughness mechanism & Appendix~\ref{app:roughness-followup-curriculum} & Inconclusive. Some models support the flatter-minima interpretation, but the pattern is not consistent. \\
\hline
\end{tabular}
\caption{Main empirical findings used in the discussion.}
\label{tab:main-findings-overview}
\end{table}

\section{Interpretation across both tracks}

Two conclusions are robust at this stage. First, curriculum gains are real but
conditional. Second, schedule and hierarchy design matter at least as much as
adding more curriculum stages.

The regression track shows that smoothing the objective can make the early task
easier, but this does not automatically translate into a better final model once
the single-target baseline is well tuned. The vision track suggests a more
promising condition: curricula are more useful when intermediate tasks preserve
meaningful structure, such as class hierarchy, while still reducing difficulty.
This difference motivates the central interpretation of the thesis: curriculum
learning is not beneficial merely because training starts with an easier task;
it is beneficial when the easier task provides information that transfers to the
final task.

\section{Answers to the research questions}

\paragraph{RQ1: objective-space continuation.}
Gaussian-inspired continuation helps on selected regression functions, but it is
not reliable after strong baseline tuning. The direct hard-target baseline wins
on several functions and always wins on runtime. The answer is therefore
conditional rather than affirmative.

\paragraph{RQ2: output-space hierarchy.}
Coarse-to-fine supervision is more promising in structured many-class vision
settings. The strongest evidence is CIFAR-100 with lower-capacity models, where
peak accuracy improves and learned hierarchies outperform matched random
hierarchies. The effect weakens for larger models and easier datasets.

\paragraph{RQ3: adaptive scheduling.}
The current adaptive methods do not replace fixed schedules. Meta-weighting in
the regression track is feasible but not better on average, and adaptive plateau
scheduling in the vision track often stays too long in coarse stages. Adaptive
scheduling remains a direction for future work rather than a completed solution.

\paragraph{RQ4: optimization mechanism.}
The diagnostic evidence is inconclusive. AUC often decreases, so curriculum does
not generally accelerate training. Roughness and sharpness probes sometimes
support a flatter-minima interpretation, especially for selected ResNet cases,
but the pattern is not consistent enough to claim a general mechanism.

\section{Limitations and threats to validity}

Some limitations:
\begin{enumerate}
    \item some promising tracks are still single-seed or low-seed and should not
          be over-generalized,
    \item many headline numbers use best-test-accuracy diagnostics and should be
          complemented by validation-selected test reporting,
    \item mechanism claims based on roughness remain suggestive rather than
          causal.
\end{enumerate}

Given these limits, the current claims are:

\begin{quote}
Curriculum learning is not a universal improvement strategy. It is most useful
in selected regimes where model capacity, hierarchy quality, and schedule
choices align with dataset structure.
\end{quote}

\chapter{Conclusions}
\label{chap:conclusions}

This thesis investigated dynamic curriculum learning in two complementary forms:
objective-space continuation for regression and output-space hierarchical
curricula for image classification. The central motivation was to move beyond
static easy-to-hard schedules and evaluate when adaptive or hierarchy-based
curricula provide measurable benefits.

The results support a nuanced conclusion. Curriculum effects are reproducible in
selected settings, but they are not universal. In the regression track,
objective-space continuation improves selected functions but does not reliably
beat tuned single-target training. In the vision track, hierarchical curricula
are more promising, especially for lower-capacity CIFAR-100 models, but the gain
still depends on dataset structure, hierarchy quality, and schedule length.
Where these conditions are not met, curriculum often increases training cost
without improving the overall trajectory.

The thesis also clarifies what can and cannot be claimed mechanistically.
Roughness and sharpness diagnostics are informative, but current evidence does
not justify a general statement that curriculum consistently finds flatter
minima. The safer interpretation is empirical: curriculum can improve the best
solution reached by training in some regimes, even when early-trajectory metrics
remain worse.

Methodologically, the work contributes a reproducible experimental workflow and
closes a key control gap through random-hierarchy ablation. In the main CIFAR-100
weak-CNN setting, learned hierarchies outperform matched random hierarchies,
which supports the claim that hierarchy quality carries useful information beyond
simple early-stage label reduction.

\section{Future work}

The next research step is consolidation rather than broad expansion. The most
valuable extensions are: (1) validation-selected evaluation for all major runs,
(2) multi-seed confirmation of the strongest adaptive-schedule candidates,
(3) broader hierarchy-quality controls across additional architectures and
datasets, and (4) tighter integration between continuation theory and practical
adaptive schedule design.

Taken together, the thesis provides a credible first-stage answer: dynamic
curriculum learning is promising, but its benefit is conditional and must be
established with careful controls, not assumed by default.

%--------------------------------------
% Literatura
%--------------------------------------

\bibliographystyle{plain}{\raggedright\sloppy\small\bibliography{bibliografia}}

%--------------------------------------
% Dodatki
%--------------------------------------

\cleardoublepage\appendix%
\newpage
\chapter{Systematic Literature Review Protocol}
\chaptermark{SLR Protocol}{}

This chapter presents the protocol for the Systematic Literature Review
(\acronym{SLR}) which serves as the foundation for the research component of
this thesis. The protocol has been developed in accordance with the
\acronym{PRISMA} guidelines \cite{page2021prisma} and the \acronym{PICOC}
framework \cite{kitchenham2007guidelines}.

\section{Review Identification}

The objective of this review is to synthesize current methodologies that
utilize \termdef{Meta-Optimization} to dynamically schedule loss functions
representing decomposed sub-tasks of varying difficulty (e.g., via function
approximation, smoothing, or auxiliary objectives) in deep learning scenarios.

\section{The PICOC Framework}

To precisely define the scope of the search, the \acronym{PICOC} framework
(Population, Intervention, Comparison, Outcome, Context) was utilized
\cite{kitchenham2007guidelines}. Details are presented in Table~\ref{tab:picoc}.

\begin{table}[ht]
	\fcmtcaption{Definition of the review scope using the PICOC framework.}\label{tab:picoc}
	\centering\footnotesize%
	\begin{tabular}{l >{\rightskip\fill}p{10cm}}
		\toprule
		Component             & Definition                                                                                                                                                               \\
		\midrule
		\textbf{P}opulation   & Deep Neural Networks (\acronym{DNN}) in Multi-Task Learning (\acronym{MTL}) settings, considering both hard and soft parameter sharing architectures.                    \\
		\textbf{I}ntervention & Adaptive Curriculum Learning (e.g., Self-Paced Learning, RL-based teachers) and Meta-Learning based loss reweighting (e.g., MAML, Learning to Reweight).                 \\
		\textbf{C}omparison   & 1. Static heuristics (Predefined curricula). \newline 2. Standard \acronym{MTL} baselines (Uniform weighting). \newline 3. Heuristic dynamic weighting (e.g., GradNorm). \\
		\textbf{O}utcome      & \textbf{Primary:} Mitigation of Negative Transfer, Generalization performance. \newline \textbf{Secondary:} Optimization stability, Convergence speed.                   \\
		\textbf{C}ontext      & Machine Learning research (excluding purely psychological curriculum studies without algorithmic implementation).                                                        \\
		\bottomrule
	\end{tabular}
\end{table}

\section{Research Questions (RQs)}

The following research questions have been defined to guide the data extraction
process:

\begin{description}
	\item[RQ1 (Curriculum)] How do adaptive approaches (e.g., Self-Paced Learning,
	      RL-based teachers) differ from static heuristics in defining training
	      schedules?
	\item[RQ2 (Multi-Task)] What strategies are employed to mitigate negative transfer
	      (e.g., gradient conflict resolution) in hard vs. soft parameter sharing
	      architectures?
	\item[RQ3 (Meta-Optimization)] How are meta-learning algorithms (e.g., MAML,
	      reweighting) applied to optimize loss weights or data selection dynamically?
\end{description}

\section{Search Strategy}

The search strategy relies on the intersection of three conceptual blocks:
\emph{Curriculum Learning}, \emph{Meta-Optimization}, and \emph{Multi-Task
	Learning}. Below are the specific queries designed for scientific databases.

\subsection{Scopus / Web of Science}

This is the primary source due to its broad indexing coverage.

\begin{verbatim}
TITLE-ABS-KEY (
  ("curriculum learning" OR "self-paced learning" OR "loss weighting" 
   OR "adaptive weighting" OR "dynamic weighting") 
  AND ("meta-learning" OR "meta-optimization" OR "learning to weight" 
   OR "gradient-based" OR "differentiable" OR "automated") 
  AND ("multi-task" OR "multi-objective" OR "auxiliary learning" 
   OR "multi-loss" OR "negative transfer")
) AND ( DOCTYPE(ar) OR DOCTYPE(cp) ) AND NOT ( "reinforcement learning" )
\end{verbatim}

\subsection{IEEE Xplore}

A stricter syntax is required here (using the \texttt{"All Metadata"}
operator).

\begin{verbatim}
("All Metadata":"curriculum learning" OR "All Metadata":"self-paced learning" 
 OR "All Metadata":"loss weighting" OR "All Metadata":"adaptive weighting" 
 OR "All Metadata":"dynamic weighting") 
AND ("All Metadata":"meta-learning" OR "All Metadata":"meta-optimization" 
 OR "All Metadata":"learning to weight" OR "All Metadata":"gradient-based" 
 OR "All Metadata":"differentiable" OR "All Metadata":"automated") 
AND ("All Metadata":"multi-task" OR "All Metadata":"multi-objective" 
 OR "All Metadata":"auxiliary learning" OR "All Metadata":"multi-loss")
\end{verbatim}

\subsection{Google Scholar}

Due to limitations in boolean logic handling, the search will be conducted
using shorter phrases:
\begin{verbatim}
"dynamic weighting" "multi task" "curriculum learning"
\end{verbatim}
Due to the large number of hits, only papers published after 2023 were
considered in the Google Scholar query.

\subsection{Snowballing Strategy}

To complement the database search and ensure comprehensive coverage, a
snowballing procedure will be applied \cite{wohlin2014guidelines}:
\begin{itemize}
	\item \textbf{Forward Snowballing:} Reviewing papers that cite seminal works identified in the supervisor's recommendations (e.g., \emph{GradNorm}, \emph{MAML}, \emph{Self-Paced Learning}) to identify recent advancements.
	\item \textbf{Backward Snowballing:} Examining the reference lists of included primary studies to identify foundational papers or missed relevant studies.
\end{itemize}

\section{Selection Criteria and Data Extraction}

The selection process will be conducted in two stages: title/abstract screening
followed by full-text review.

\begin{figure}[t]
	\centering
	\resizebox{0.95\textwidth}{!}{
		\input{figures/prisma_flow.tex}
	}
	\fcmfcaption{Schematic of the PRISMA selection process.}\label{rys:prisma}
\end{figure}

\paragraph{Inclusion Criteria.} The review will include papers that:
\begin{enumerate}
	\item Propose a method for \emph{automating} the training process (adaptive
	      curriculum or loss weighting).
	\item Address Multi-Task Learning challenges, specifically Negative Transfer or
	      parameter sharing.
	\item Utilize Meta-Learning, Self-Paced Learning, or Reinforcement Learning
	      approaches.
	\item Were published between 2017 and 2025.
\end{enumerate}

\paragraph{Data Extraction.} The following information will be extracted from selected papers:
\begin{itemize}
	\item \textbf{Metadata:} Title, year, publication venue.
	\item \textbf{Methodology:} Type of Curriculum (Static/Adaptive/SPL) and Optimization strategy.
	\item \textbf{Architecture:} Hard vs. Soft parameter sharing.
	\item \textbf{Results:} Mitigation of negative transfer and generalization improvements.
\end{itemize}

\input{appendices/sweep_pack_v1}
\input{appendices/curriculum_vs_single_benchmark_v1}
\input{appendices/sweep_pack_v3_ratio_setup}
\input{appendices/meta_weighting_eggholder_u1}
\input{appendices/cifar100_model_size_curriculum_wandb}
\input{appendices/roughness_followup_curriculum}
\input{appendices/teacher_transfer_probe}
\input{appendices/hierarchy_ablation_curriculum}
\input{appendices/deep_dive_experiments}

\ppcolophon

\end{document}
