[build-system]
requires = ["flit_core >=3.2,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "ma_thesis"
version = "0.0.1"
description = "MA Thesis"
authors = [
  { name = "Adam Korba" },
]
license = { file = "LICENSE" }
readme = "README.md"
classifiers = [
    "Programming Language :: Python :: 3",
    "License :: OSI Approved :: MIT License"
]
dependencies = [
    "flax>=0.10.7",
    "jax>=0.6.2",
    "lightning>=2.5",
    "loguru",
    "marimo>=0.18.1",
    "matplotlib>=3.10.7",
    "mlflow>=2.12",
    "mkdocs",
    "numpy>=2.2.6",
    "optuna>=4.0",
    "pandas>=2.3.2",
    "polars>=1.36.1",
    "pyyaml>=6.0.2",
    "python-dotenv",
    "ruff",
    "scikit-learn>=1.6",
    "scipy>=1.15",
    "tqdm",
    "typer",
    "torch>=2.10.0",
    "torchvision>=0.21",
]
requires-python = "~=3.10.0"

[project.scripts]
ma-exp = "ma_thesis.experiment:app"


[tool.ruff]
line-length = 99
src = ["ma_thesis"]
include = ["pyproject.toml", "ma_thesis/**/*.py"]

[tool.ruff.lint]
extend-select = ["I"]  # Add import sorting

[tool.ruff.lint.isort]
known-first-party = ["ma_thesis"]
force-sort-within-sections = true
